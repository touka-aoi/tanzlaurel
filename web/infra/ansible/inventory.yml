all:
  hosts:
    blog-vm:
      ansible_host: "{{ vm_ip }}"
      ansible_user: "{{ vm_user | default('deploy') }}"
      ansible_ssh_private_key_file: "{{ ssh_key_path | default('~/.ssh/id_ed25519') }}"
